# FastAPI and server
fastapi==0.128.0
uvicorn==0.32.0

# Database
sqlalchemy==2.0.36
aiosqlite==0.19.0

# Authentication
authlib==1.6.6
httpx==0.26.0
itsdangerous==2.1.2
python-multipart==0.0.22
cryptography==43.0.3  # Pin to 43.x - authlib uses deprecated API removed in 44+

# Environment variables
python-dotenv==1.0.1

# YAML parsing
pyyaml==6.0.1

# AI course extension (Build Real Stuff) - calls route through OpenRouter
# (openrouter.ai), which is OpenAI-compatible, so the official openai
# client works against it by pointing base_url at
# https://openrouter.ai/api/v1. See docs/AI-COURSE-EXTENSION-PLAN.md
# section 6.
openai==2.44.0

# Testing (dev-only, but there wasn't a test framework in this file at all
# before the AI course extension -- see docs/AI-COURSE-BUILD-PLAN.md
# Milestone 1. backend/tests/ uses this + FastAPI's built-in TestClient.
pytest==9.1.1

# Note: FastAPI includes starlette dependency (>=0.49.1 required for security)
